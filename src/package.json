{
  "name": "fetch-send-request",
  "version": "1.0.1",
  "description": "A wrapper around fetch() with callbacks",
  "main": "./build/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hughbe/fetch-send-request.git"
  },
  "keywords": [
    "fetch",
    "send",
    "request"
  ],
  "author": "Hugh Bellamy",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/hughbe/fetch-send-request/issues"
  },
  "homepage": "https://github.com/hughbe/fetch-send-request#readme",
  "dependencies": {
    "babel-polyfill": "^6.23.0",
    "moment": "^2.18.1",
    "whatwg-fetch": "^2.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.6.0"
  },
  "scripts": {
    "build": "babel lib --presets env --out-dir build",
    "prepublish": "npm run build"
  }
}
